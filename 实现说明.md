# InternScreen 实现说明

## 实现方式

**技术栈**：LangGraph 1.2 + FastAPI + React + qwen-3.8-max

后端把简历筛选流程编排成一个有状态的 LangGraph：parse → extract → validate → screen → explain 五个节点，中间由条件路由串联。前端是单页看板，输入简历文本或 PDF，调用 `/api/agent/run` 启动图，流式拉取节点日志。SQLite 存历史，LangGraph MemorySaver 做图状态 checkpoint。

## 为什么用 LangGraph 而不是纯 LLM Prompt Chain

核心原因是**需要人工介入**。简历筛选不是一次性任务——字段缺失、院校层次冲突时必须停下来问人。LangGraph 的 `interrupt()` + `Command(resume=...)` 机制天然支持：图执行到一半自动暂停、保存完整状态、等前端回传人工输入后从断点继续，已跑节点不重算。换成纯 Chain 就得自己造状态管理，复杂度高且易丢上下文。

另外，院校层次判定**不能靠 LLM**（同一份简历两次结果可能不一样）。我用了双一流名单（2022 教育部官方 147 所 + 109 个别名）做确定性匹配，名单是**最高权威**——一旦命中直接采用，忽略 LLM 判断；名单未命中才看 LLM 兜底；只有两边都没结论时才触发复核。同时配了后缀规则处理异地校区（算双一流）和独立学院（算非双一流）。这套逻辑刚好能被 LangGraph 的条件路由清晰表达。

## AI 承担的步骤

- **解析**：PDF 用 pdfplumber、文本直接用，产出 clean text
- **结构化抽取**：LangChain `with_structured_output` 产 12+ 字段（学历、院校名、AI 工具经历、时长等）
- **打分依据推断**：规则引擎按字段查 YAML 配置算分
- **解释生成**：最后把硬性格拒原因 / 分数拆解拼成自然语言

## 必须人把关的步骤

1. **字段缺失补全**：LLM 没抽到可实习时长 / 到岗天数 / 成都线下 → 弹复核表单
2. **院校层次无法确认**：名单未命中且 LLM 也没给 tier（两边都无结论）→ 人工选 tier
3. **最终录取决策**：系统只给「推荐 / 备选 / 暂不推进」三级建议，**是否发笔试题库由 HR 说了算**
